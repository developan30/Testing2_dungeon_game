# Agent Instructions

## Commands
- Install deps: `npm install`
- Dev server: `npm run dev`
- Build: `npm run build`
- Lint: `npm run lint`

## Notes
- Keep gameplay in `src/game`.
- Keep scene/ui components in `src/components`.
- Ensure `npm run build` passes before finishing.
